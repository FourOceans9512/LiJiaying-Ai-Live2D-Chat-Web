# Ai-Live2D Chat Web — 项目规格说明书

> **版本**: v1.0  
> **日期**: 2026-08-22  
> **状态**: 已确认  
> **协议**: AGPL-3.0

---

## 目录

1. [项目概述](#1-项目概述)
2. [范围定义](#2-范围定义)
3. [系统架构](#3-系统架构)
4. [技术规格](#4-技术规格)
5. [Agent 宪法与人格设计](#5-agent-宪法与人格设计)
6. [数据架构](#6-数据架构)
7. [部署架构](#7-部署架构)
8. [交付物清单](#8-交付物清单)
9. [里程碑与路线图](#9-里程碑与路线图)
10. [质量标准](#10-质量标准)
11. [团队与协作](#11-团队与协作)
12. [约束与合规](#12-约束与合规)
13. [风险与降级](#13-风险与降级)
14. [Skill 集成计划](#14-skill-集成计划)
15. [附录](#15-附录)

---

## 1. 项目概述

### 1.1 项目目标

构建一个基于 Live2D 的 AI 虚拟角色 Web 聊天应用，面向个人使用并开源至社区，追求尽可能高的 GitHub Stars 和社区影响力。

### 1.2 核心价值

- **本地化优先**: 数据存储在本地，API Key 由用户自备，最大程度保护隐私
- **开源透明**: AGPL-3.0 协议，社区驱动
- **多模型支持**: 支持 OpenAI / 国内 API / 本地模型，用户手动切换
- **Live2D 沉浸体验**: 角色 Live2D 渲染 + 语音对话 + 口型同步

### 1.3 成功指标

| 指标 | 目标 |
|------|------|
| GitHub Stars | 尽可能高 |
| 开箱即用 | 内置示例角色，Clone 即可运行 |
| 社区贡献 | 接受 PR，有清晰的贡献指南 |

### 1.4 长期路线图

| 阶段 | 时间 | 交付 |
|------|------|------|
| Phase 1 | 0-3 个月 | Web 网页文字对话版 |
| Phase 2 | 3-6 个月 | Web 网页语音对话版（TTS + ASR） |
| Phase 3 | 6-12 个月 | Windows 桌面端（Electron），若可行同步开发 Android 移动端 |
| Phase 4 | 12 个月+ | 个性化固件硬件端（参考小智 AI 形式） |

---

## 2. 范围定义

### 2.1 功能优先级

#### P0 — 核心功能（Phase 1 必须完成）

| 功能 | 描述 |
|------|------|
| 多模型切换 | 用户在设置页选择 LLM（OpenAI / 豆包 / 通义千问 / 文心 / 智谱 / DeepSeek / Ollama 等），手动切换，切换时保留上下文 |
| 文本对话 | 用户输入文本，LLM 返回结构化 JSON（情绪 + 动作 + 表情 + 回复文本） |
| 多轮上下文 | 维护对话历史，上下文窗口根据模型能力决定或用户自定义 |
| 性格设定 | 用户自定义角色人格（名字、性格、语气、身份背景、口头禅、禁忌话题） |
| 聊天记录持久化 | 聊天记录存储到本地（IndexedDB + SQLite），支持历史查看和导出 |

#### P0 — 核心功能（Phase 2 追加）

| 功能 | 描述 |
|------|------|
| TTS 语音合成 | 火山引擎 TTS，每角色独立音色，整句合成后播放 |
| ASR 语音输入 | 火山引擎 ASR，实时语音转文字 |
| Live2D 口型同步 | 基于音量包络驱动 Live2D 嘴部参数，与 TTS 音频同步 |

#### P1 — 增强功能

| 功能 | 描述 |
|------|------|
| Live2D 模型渲染 | pixi-live2d-display，支持 Cubism 2/4/5 格式（moc3, model3.json, mtn） |
| 动作/表情触发 | 根据对话情绪分类 + 关键词触发 Live2D 动作和表情 |
| 交互方式 | 鼠标悬停 / 点击 / 拖拽 / 键盘快捷键 / 语音激发 / 文本对话激发 |
| Function Calling | LLM 可调用外部工具（查天气、设提醒等） |
| 多角色支持 | 初版 1 个角色，后期可扩展至上限 5 个，用户自定义人格 |

#### P2 — 规划功能（后期迭代）

| 功能 | 描述 |
|------|------|
| RAG 知识库 | 向量数据库存储角色背景故事/知识，对话时检索注入 |
| 聊天记忆 RAG | 将聊天记录向量化，实现跨对话长期记忆 |
| 未成年人模式 | 内容过滤加强，声明不针对未成年人 |
| 云端同步 | 可选的云端聊天记录同步（需引入鉴权体系） |

### 2.2 不在范围（Out of Scope — 第一版）

- 移动端 App（Phase 3 考虑）
- VR/AR 支持
- 多人聊天室
- 付费墙 / 订阅系统
- 用户登录注册系统（第一版纯本地）

---

## 3. 系统架构

### 3.1 架构总览

```
┌─────────────────────────────────────────────────────┐
│                    浏览器 (前端)                      │
│  ┌───────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ React UI  │  │ Live2D   │  │ IndexedDB       │  │
│  │ Chat界面   │  │ 渲染层    │  │ 本地存储         │  │
│  └─────┬─────┘  └────┬─────┘  └────────┬─────────┘  │
│        │              │                 │             │
│  ┌─────┴──────────────┴─────────────────┴──────┐    │
│  │           前端状态管理 (Zustand)              │    │
│  └───────────────────┬──────────────────────────┘    │
└──────────────────────┼──────────────────────────────┘
                       │ HTTPS API
┌──────────────────────┼──────────────────────────────┐
│              Node.js + Fastify (后端)                 │
│  ┌───────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ API Gateway│  │ 密钥代理  │  │ SQLite          │  │
│  │ 路由/限流   │  │ 安全转发  │  │ 持久化存储       │  │
│  └─────┬─────┘  └────┬─────┘  └──────────────────┘  │
│        │             │                               │
│  ┌─────┴─────────────┴──────────────────────────┐   │
│  │           内容安全中间件 (关键词过滤)           │   │
│  └───────────────────┬──────────────────────────┘   │
└──────────────────────┼──────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
   ┌────┴────┐   ┌────┴────┐   ┌────┴────┐
   │  LLM    │   │  TTS    │   │  ASR    │
   │ 多模型   │   │ 火山引擎 │   │ 火山引擎 │
   └─────────┘   └─────────┘   └─────────┘
```

### 3.2 数据流

#### 文本对话流程

```
用户输入文本
  → 前端发送到后端 API Gateway
  → 后端组装 System Prompt + 上下文 + 用户消息
  → 调用 LLM API (用户选择的模型)
  → LLM 返回结构化 JSON: { emotion, action, expression, text }
  → 后端内容安全检查 (关键词过滤)
  → 返回前端
  → 前端解析 JSON
    → 显示文本
    → 触发 Live2D 动作/表情
  → 持久化到 IndexedDB + SQLite
```

#### 语音对话流程

```
用户语音输入
  → 浏览器录音 → 火山引擎 ASR 转文字
  → [同文本对话流程]
  → LLM 返回结构化 JSON
  → 后端调用火山引擎 TTS 合成语音
  → 返回音频流 + JSON 到前端
  → 前端播放音频
    → 音量包络分析 → 驱动 Live2D 口型同步
  → 同时显示文本 + 触发动作/表情
```

### 3.3 LLM 对话结构

LLM 返回结构化 JSON：

```json
{
  "emotion": "happy",
  "action": "greeting",
  "expression": "smile",
  "text": "你好呀！很高兴见到你～",
  "tool_calls": []
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `emotion` | string | 情绪分类: happy/sad/angry/surprised/neutral/shy 等 |
| `action` | string | 动作名: greeting/wave/nod/shake/head_tilt 等 |
| `expression` | string | 表情名: smile/frown/surprise/blush 等 |
| `text` | string | 回复文本 |
| `tool_calls` | array | Function Calling 结果（查天气、设提醒等） |

---

## 4. 技术规格

### 4.1 前端技术栈

| 项目 | 选型 | 说明 |
|------|------|------|
| 框架 | React 18+ | 生态最大，Live2D 库支持最好 |
| 语言 | TypeScript | 类型安全，提升代码质量 |
| 构建 | Vite | 快速热更新，ESM 原生支持 |
| 状态管理 | Zustand | 轻量，TypeScript 友好 |
| 路由 | React Router | 标准路由方案 |
| 样式 | Tailwind CSS | 快速开发，一致性设计 |
| Live2D | pixi-live2d-display | MIT 协议，支持 Cubism 2/4/5 |
| 渲染引擎 | PixiJS | pixi-live2d-display 底层依赖 |
| 本地存储 | IndexedDB (via Dexie.js) | 浏览器端持久化 |
| HTTP 客户端 | fetch / ky | 轻量 HTTP 请求 |
| 测试 | Vitest + React Testing Library | 单元测试 + 组件测试 |

### 4.2 后端技术栈

| 项目 | 选型 | 说明 |
|------|------|------|
| 运行时 | Node.js 20 LTS | 稳定长期支持 |
| 框架 | Fastify | 高性能，插件生态好 |
| 语言 | TypeScript | 前后端同语言 |
| 数据库 | SQLite (via better-sqlite3) | 本地化，零配置，单文件 |
| ORM | Drizzle ORM | 轻量，TypeScript 优先 |
| 验证 | Zod | Schema 验证，前后端共享 |
| 日志 | Pino | 高性能日志 |
| 测试 | Vitest + Supertest | API 测试 |

### 4.3 Live2D 规格

| 项目 | 选型 |
|------|------|
| SDK | pixi-live2d-display |
| 支持格式 | Cubism 2 (moc, mtn, model.json), Cubism 4/5 (moc3, model3.json) |
| 渲染引擎 | PixiJS (pixi-live2d-display 依赖) |
| 口型同步 | 音量包络驱动 (Volume Envelope Lip-Sync) |
| 动作触发 | 情绪分类 + 关键词触发 |
| 交互 | 鼠标悬停/点击/拖拽 + 键盘快捷键 + 语音/文本激发 |
| 模型来源 | 用户自定义（开源模型/商业授权/AI建模），版权由用户承担 |

#### 口型同步实现

```
TTS 音频播放
  → Web Audio API AnalyserNode 实时分析音频音量
  → 计算音量包络 (RMS)
  → 映射到 Live2D ParamMouthOpenY 参数 (0.0 ~ 1.0)
  → 每帧更新 Live2D 模型参数
  → 实现口型与语音同步
```

### 4.4 AI 对话层规格

#### 支持的 LLM

| 分类 | 模型 | 接入方式 |
|------|------|----------|
| OpenAI | GPT-4o / GPT-4o mini / o1 | OpenAI API (兼容格式) |
| 国内 API | 豆包 / 通义千问 / 文心 / 智谱 / DeepSeek | 各厂商 API (OpenAI 兼容格式优先) |
| 本地模型 | Ollama / Llama.cpp / vLLM | OpenAI 兼容 API (localhost) |

#### 模型路由策略

- **手动切换**: 用户在设置页选择当前 LLM
- 切换时保留对话上下文
- 每个模型配置独立的: API地址 / API Key / 模型名 / 上下文窗口大小
- 统一使用 OpenAI 兼容格式调用，非兼容厂商通过适配器转换

#### 上下文策略

| 项目 | 说明 |
|------|------|
| 上下文窗口 | 根据模型能力决定或用户自定义 |
| 超长对话处理 | 第一版: 截断早期消息 (保留最近 N 条) |
| 长期记忆 (P2) | 向量记忆 RAG (后期实现) |

#### System Prompt 结构

```
[角色人格设定]
  - 名字、性格、语气、身份背景、口头禅

[行为边界]
  - 违法内容过滤规则
  - 自残/自杀防护规则
  - 隐私信息保护规则
  - 政治敏感话题处理规则

[输出格式]
  - 必须返回结构化 JSON: { emotion, action, expression, text }
  - 可选 Function Calling

[对话上下文]
  - 最近 N 轮对话历史
```

### 4.5 语音层规格

#### TTS (火山引擎)

| 项目 | 说明 |
|------|------|
| 引擎 | 火山引擎 TTS |
| 音色 | 每角色独立音色，用户可配置音色 ID |
| 合成方式 | 整句合成后播放 |
| 接入 | 后端代理调用，API Key 存于后端环境变量 |

#### ASR (火山引擎)

| 项目 | 说明 |
|------|------|
| 引擎 | 火山引擎 ASR |
| 模式 | 实时流式识别 |
| 接入 | 后端 WebSocket 代理或浏览器直连 |

---

## 5. Agent 宪法与人格设计

### 5.1 宪法模板

基于参考仓库 (`gitee.com/sliver-ring_admin/skills`) 中的 `AGENTS.md` 和 `AI_USAGE.md` 定制，不复制粘贴，由 AI 结合仓库说明生成定制版。

### 5.2 宪法优先级

```
安全合规 > 角色一致性 > 帮助用户 > 诚实 > 其他
```

### 5.3 行为边界（红线）

| 红线 | 规则 |
|------|------|
| 违法内容过滤 | 禁止生成暴力、色情、毒品、恐怖主义等违法内容 |
| 自残/自杀防护 | 禁止自残/自杀相关讨论，检测到时提供求助热线 |
| 隐私信息保护 | 禁止泄露用户隐私信息（地址/电话/身份证等），检测到时提醒用户 |
| 政治敏感话题 | 禁止讨论或保持中立，用户可在角色设定中自定义开关 |

### 5.4 内容安全实现

- 后端 API Gateway 层增加轻量关键词过滤中间件
- 可配置词表
- 对 LLM 输出做后置检查（不对用户输入做强制过滤，避免影响体验）
- 命中时拦截并返回预设安全回复

### 5.5 角色设计

| 项目 | 说明 |
|------|------|
| 初版角色数 | 1 个 |
| 上限 | 5 个 |
| 人格自定义 | 用户自定义: 名字、性格、语气、身份背景、口头禅、禁忌话题 |
| 内置示例 | 提供至少 1 个开箱即用的示例角色 + 示例对话 + 示例人格设定 |

---

## 6. 数据架构

### 6.1 存储策略

**核心原则: 尽可能本地化存储**

| 数据类型 | 存储位置 | 说明 |
|----------|----------|------|
| 聊天记录 | IndexedDB (前端) + SQLite (后端) | 双写，前端为主，后端备份 |
| 用户配置 | IndexedDB | 角色设定、模型配置、UI偏好 |
| 模型元数据 | SQLite | 模型配置、API地址、密钥（加密存储） |
| Live2D 模型文件 | 浏览器本地缓存 (Cache API / OPFS) | 模型文件较大，需缓存 |
| 上传资源 | 本地文件系统 (后端) / OPFS | 用户上传的模型、图片等 |

### 6.2 数据库 Schema (SQLite)

```
conversations (对话表)
  - id: TEXT PRIMARY KEY
  - role_id: TEXT
  - model_id: TEXT
  - created_at: DATETIME
  - updated_at: DATETIME

messages (消息表)
  - id: TEXT PRIMARY KEY
  - conversation_id: TEXT FK
  - role: TEXT (user/assistant/system)
  - content: TEXT
  - emotion: TEXT
  - action: TEXT
  - expression: TEXT
  - tool_calls: TEXT (JSON)
  - created_at: DATETIME

characters (角色表)
  - id: TEXT PRIMARY KEY
  - name: TEXT
  - personality: TEXT
  - tone: TEXT
  - background: TEXT
  - catchphrase: TEXT
  - taboos: TEXT
  - tts_voice_id: TEXT
  - live2d_model_path: TEXT
  - created_at: DATETIME
  - updated_at: DATETIME

model_configs (模型配置表)
  - id: TEXT PRIMARY KEY
  - provider: TEXT (openai/doubao/qwen/ollama...)
  - api_url: TEXT
  - api_key_encrypted: TEXT
  - model_name: TEXT
  - context_window: INTEGER
  - is_active: BOOLEAN
  - created_at: DATETIME

settings (系统设置表)
  - key: TEXT PRIMARY KEY
  - value: TEXT
```

### 6.3 鉴权与密钥

| 项目 | 说明 |
|------|------|
| LLM API Key | 用户自备，存于后端环境变量或加密存入 SQLite |
| TTS/ASR Key | 火山引擎 API Key，存于后端环境变量 |
| 前端传输 | API Key 不暴露给浏览器，后端代理转发 |
| 加密 | SQLite 中的密钥字段使用 AES-256 加密存储 |

---

## 7. 部署架构

### 7.1 部署方案

```
┌──────────────────────────────────┐
│         前端 (静态托管)           │
│  Vercel / Cloudflare Pages        │
│  自动构建 + CDN 加速              │
│  免费额度足够个人项目             │
└──────────────┬───────────────────┘
               │ HTTPS API
┌──────────────┼───────────────────┐
│      后端 (Docker 容器)          │
│  Docker + Node.js + Fastify       │
│  部署在本地 / 云服务器            │
│  数据: SQLite (容器卷映射)        │
└──────────────────────────────────┘
```

### 7.2 前端部署

| 项目 | 说明 |
|------|------|
| 平台 | Vercel 或 Cloudflare Pages |
| 构建 | `npm run build` → `dist/` |
| 自动部署 | GitHub Push 触发自动构建 |
| 自定义域名 | 可选 |

### 7.3 后端部署

| 项目 | 说明 |
|------|------|
| 方式 | Docker 容器 |
| 镜像 | `node:20-alpine` 基础镜像 |
| 数据卷 | SQLite 文件映射到宿主机 |
| 环境变量 | API Key 等敏感信息通过 `.env` 或 Docker Secrets |
| 端口 | 默认 3001，可配置 |

### 7.4 未来桌面端 (Phase 3)

| 项目 | 说明 |
|------|------|
| 框架 | Electron (与 Web 版共享 React 前端) |
| 后端 | 内嵌 Node.js 进程 (复用 Fastify 后端) |
| 数据 | 本地 SQLite 文件 |
| 打包 | electron-builder，Windows .exe / .msi |

---

## 8. 交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | Web 应用源码 | 完整前端 + 后端源码 |
| 2 | 构建产物 | `dist/` 静态包 |
| 3 | 部署脚本 | Dockerfile + docker-compose.yml + CI/CD 配置 |
| 4 | Live2D 角色示例 | 至少 1 个可运行的示例角色（含模型 + 人格设定 + 示例对话） |
| 5 | 配置文档 | 如何替换角色、切换 LLM、配置 TTS/ASR、添加 Skill |
| 6 | 开发者贡献指南 | CONTRIBUTING.md + README.md |
| 7 | 测试 | 单元测试 (Vitest) + E2E 测试 (Playwright) |
| 8 | Demo | 视频或截图展示核心功能 |
| 9 | 示例数据 | 内置示例角色 + 示例对话 + 示例人格设定，开箱即用 |

---

## 9. 里程碑与路线图

### Phase 1: Web 文字对话版 (0-3 个月)

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| M1.1 | 项目初始化 + 技术架构搭建 | React + Vite + Fastify 骨架跑通，前后端通信正常 |
| M1.2 | LLM 多模型切换 | 支持 OpenAI / DeepSeek / Ollama，设置页可切换 |
| M1.3 | 文本对话 + 多轮上下文 | 用户输入文本 → LLM 返回结构化 JSON → 显示回复 |
| M1.4 | 性格设定 + System Prompt | 用户自定义角色人格，注入 System Prompt |
| M1.5 | 聊天记录持久化 | IndexedDB + SQLite 双写，历史查看和导出 |
| M1.6 | Live2D 渲染基础 | pixi-live2d-display 集成，模型加载 + 基础交互 |
| M1.7 | 内容安全中间件 | 关键词过滤，4 项红线规则 |
| M1.8 | 部署 + 文档 | Docker 部署 + README + 配置文档 |
| M1.9 | 测试 + Demo | 单元测试 + E2E 测试 + Demo 视频 |

### Phase 2: Web 语音对话版 (3-6 个月)

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| M2.1 | 火山引擎 TTS 集成 | 文字 → 语音合成，整句播放 |
| M2.2 | 火山引擎 ASR 集成 | 语音输入 → 文字识别 |
| M2.3 | Live2D 口型同步 | 音量包络驱动口型，与 TTS 同步 |
| M2.4 | 动作/表情联动 | LLM 返回情绪/动作 → 触发 Live2D 动作/表情 |
| M2.5 | 多角色支持 | 支持切换角色，每角色独立音色 |
| M2.6 | Function Calling | LLM 可调用外部工具 |
| M2.7 | 优化 + 测试 | 性能优化 + 完整测试 |

### Phase 3: 桌面端 (6-12 个月)

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| M3.1 | Electron 打包 | Web 版打包为 Windows 桌面应用 |
| M3.2 | 本地后端内嵌 | Fastify 作为内嵌进程运行 |
| M3.3 | 本地文件管理 | Live2D 模型/资源本地管理 |
| M3.4 | 安装包 | .exe / .msi 安装包 |
| M3.5 | Android (可选) | 若可行，同步开发 Android 移动端 |

### Phase 4: 硬件端 (12 个月+)

| 里程碑 | 内容 |
|--------|------|
| M4.1 | 硬件选型 + 固件架构 |
| M4.2 | 语音交互 (离线/在线) |
| M4.3 | Live2D 替代显示方案 (LCD/OLED) |
| M4.4 | 个性化外观定制 |

---

## 10. 质量标准

### 10.1 性能指标

| 指标 | 目标 |
|------|------|
| 首屏加载 | < 3 秒 (CDN) |
| 对话响应 (首字) | < 2 秒 (取决于 LLM) |
| TTS 首音延迟 | < 1.5 秒 |
| ASR 识别延迟 | < 1 秒 |
| Live2D 渲染 FPS | ≥ 30 FPS (理想 60 FPS) |
| 并发用户 | 单实例支持 10+ 并发对话 |

### 10.2 浏览器兼容性

| 浏览器 | 支持版本 |
|--------|----------|
| Chrome | 最新 2 个版本 |
| Edge | 最新 2 个版本 |
| Firefox | 最新 2 个版本 |
| Safari | 最新 2 个版本 |
| 移动端浏览器 | Phase 3 考虑 |

### 10.3 代码质量

| 项目 | 标准 |
|------|------|
| 语言 | TypeScript 严格模式 |
| Lint | ESLint + Prettier |
| 单元测试覆盖率 | ≥ 70% 核心模块 |
| E2E 测试 | Playwright 覆盖核心流程 |
| 提交规范 | Conventional Commits |
| Code Review | PR 必须经过 Review |

### 10.4 响应式设计

- 第一版: 桌面端优先 (1280px+)
- 适配: 平板 (768px+) 和手机 (375px+) 基本可用
- Phase 3: 移动端深度适配

### 10.5 国际化

| 阶段 | 语言 |
|------|------|
| Phase 1 | 简体中文 |
| Phase 2+ | English / 繁体中文 / 日文 (按需) |

---

## 11. 团队与协作

### 11.1 团队构成

| 角色 | 人数 | 负责范围 |
|------|------|----------|
| 产品/全栈开发 | 1 人 (你) | 需求决策 + 前后端开发 + AI/Prompt 工程 + 测试 |
| Live2D 模型/美术 | 未定 | 角色模型制作（后期或社区贡献） |

### 11.2 协作流程

| 项目 | 工具 |
|------|------|
| 代码托管 | GitHub |
| Issue 管理 | GitHub Issues |
| PR + Code Review | GitHub Pull Requests |
| CI/CD | GitHub Actions |
| 沟通 | 邮件 + Microsoft Teams |

### 11.3 Git 工作流

```
main (生产分支)
  ├── develop (开发分支)
  │    ├── feature/xxx (功能分支)
  │    ├── fix/xxx (修复分支)
  │    └── refactor/xxx (重构分支)
  └── release/x.x (发布分支)
```

- `main`: 可部署状态，打 Tag 发布
- `develop`: 开发集成分支
- `feature/*`: 功能开发分支，完成后 PR 合入 develop
- 所有 PR 需通过 CI (lint + test) + Code Review

---

## 12. 约束与合规

### 12.1 预算约束

暂无硬性预算约束。

### 12.2 隐私合规

| 项目 | 说明 |
|------|------|
| 数据存储 | 所有数据存储在本地 (IndexedDB + SQLite)，不上传到任何服务器 |
| 隐私声明 | README 中声明"所有数据存储在本地，不上传到任何服务器" |
| API Key | 用户自备，存于本地，不收集不传输 |
| 未来云端同步 | 若引入，则需 GDPR / 个人信息保护法合规框架 |

### 12.3 未成年人模式

- 第一版: 不针对未成年人，README 中声明
- P2 功能: 后期加入未成年人模式

### 12.4 内容安全

- 后端关键词过滤中间件 (可配置词表)
- 对 LLM 输出做后置检查
- 命中红线时返回预设安全回复

### 12.5 开源协议

| 项目 | 说明 |
|------|------|
| 协议 | AGPL-3.0 |
| 要求 | 修改后的网络服务也必须开源 |
| 适用 | 源码 + 衍生作品 |

### 12.6 版权声明

- Live2D 模型版权由用户承担
- 项目本身使用 AGPL-3.0
- 依赖库各自遵循其协议

---

## 13. 风险与降级

### 13.1 风险矩阵

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| API 成本超支 | 高 | 中 | 支持本地模型 fallback，用户自备 Key |
| Live2D 授权问题 | 中 | 低 | 使用 pixi-live2d-display (MIT)，模型版权用户承担 |
| 对话质量不稳定 | 中 | 中 | Prompt 工程 + 结构化输出 + 后置检查 |
| 火山引擎服务变更 | 中 | 低 | 抽象 TTS/ASR 接口，可切换引擎 |
| 浏览器兼容性 | 低 | 中 | 优先 Chrome/Edge，渐进增强 |

### 13.2 降级方案

| 故障场景 | 降级方案 |
|----------|----------|
| LLM 不可用 | 优先切换本地模型 (Ollama)，预设回复兜底 |
| TTS 不可用 | 降级为纯文字回复 |
| ASR 不可用 | 降级为纯文本输入 |
| Live2D 加载失败 | 降级为静态头像 + 纯聊天界面 |
| 后端不可用 | 前端可直连 LLM API (降级模式，需用户在前端配置 Key) |

---

## 14. Skill 集成计划

### 14.1 已选 Skill

| Skill | 来源 | 用途 |
|-------|------|------|
| superpowers | github.com/obra/superpowers | 完整开发工作流: 计划/TDD/调试/代码审查 |
| pua | github.com/tanweai/pua | 高能动性/反摆烂 agent 驱动 |
| define-goal | OpenAI curated | 把模糊目标澄清成可衡量、可执行的目标 |
| speech | OpenAI curated | 通过 OpenAI Audio API 生成旁白/语音提示/批量 TTS |
| transcribe | OpenAI curated | 转写音频/视频，可带说话人提示和 diarization |
| playwright | OpenAI curated | 从终端自动化浏览器，做 UI 流程调试和截图 |
| security-best-practices | OpenAI curated | 对 Python/JS/TS/Go 做安全最佳实践审查 |
| security-threat-model | OpenAI curated | 对代码库做威胁建模 |

### 14.2 推荐追加 Skill

| Skill | 来源 | 用途 | 理由 |
|-------|------|------|------|
| writing-skills | superpowers 包含 | 编写项目自定义 Skill | 如角色管理 Skill |
| jupyter-notebook | OpenAI curated | 创建/编辑 Jupyter Notebook | AI 对话调试和 Prompt 测试 |
| gh-address-comments | OpenAI curated | 处理 GitHub PR/Issue 评论 | 开源后社区协作 |
| gh-fix-ci | OpenAI curated | 排查 GitHub Actions CI 失败 | CI 自动化 |
| yeet | OpenAI curated | 自动化 stage/commit/push/PR | 发布流程自动化 |
| migrate-to-codex | OpenAI curated | 配置迁移到 Codex 格式 | 项目配置管理 |

### 14.3 Agent 宪法技能

| Skill | 来源 | 用途 |
|-------|------|------|
| writing-plans | superpowers 包含 | 编写开发计划 |
| executing-plans | superpowers 包含 | 执行开发计划 |
| systematic-debugging | superpowers 包含 | 系统化调试 |
| verification-before-completion | superpowers 包含 | 完成前验证 |

---

## 15. 附录

### 15.1 参考资源

| 资源 | 链接 |
|------|------|
| Skill 仓库 (Agent 宪法模板) | https://gitee.com/sliver-ring_admin/skills |
| OpenAI Skills 仓库 | https://github.com/openai/skills/tree/main/skills/.curated |
| superpowers | https://github.com/obra/superpowers |
| pua | https://github.com/tanweai/pua |
| pixi-live2d-display | https://github.com/guansss/pixi-live2d-display |
| OpenBeetles 社区 | https://www.openbeetles.com/ |

### 15.2 术语表

| 术语 | 说明 |
|------|------|
| Live2D | 2D 角色动画技术，通过参数控制角色表情和动作 |
| Cubism | Live2D 的核心建模和动画 SDK 版本系列 |
| moc3 | Cubism 4/5 的模型文件格式 |
| TTS | Text-to-Speech，文本转语音 |
| ASR | Automatic Speech Recognition，自动语音识别 |
| LLM | Large Language Model，大语言模型 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Function Calling | LLM 调用外部工具/函数的能力 |
| Lip-sync | 口型同步，角色嘴部动作与语音同步 |
| API Gateway | API 网关，统一处理请求路由、鉴权、限流 |

### 15.3 技术选型决策记录

| 决策项 | 选型 | 决策理由 |
|--------|------|----------|
| 前端框架 | React + TypeScript | 生态最大，Live2D 库支持最好，开源贡献门槛低 |
| 后端框架 | Node.js + Fastify | TS 全栈，API Gateway + 密钥代理 + SQLite，未来 Electron 可复用 |
| Live2D SDK | pixi-live2d-display | MIT 协议，支持 Cubism 2/4/5，社区最活跃 |
| TTS 引擎 | 火山引擎 TTS | 中文音色最佳，支持音色克隆，付费便宜 |
| ASR 引擎 | 火山引擎 ASR | 中文识别最佳，与 TTS 同平台集成简单 |
| 口型同步 | 音量包络驱动 | 简单可靠，性能好，视觉效果可接受 |
| 对话结构 | 结构化 JSON + Function Calling | 支持情绪/动作/表情联动 + Tool Call |
| 模型路由 | 手动切换 | 用户在设置页选择，简单可靠 |
| RAG | 第一版不需要 | System Prompt 注入角色设定，后期扩展 |
| 部署 | 静态前端 + Docker 后端 | 前端免费 CDN，后端灵活部署 |
| 数据存储 | IndexedDB + SQLite | 尽可能本地化 |
| 开源协议 | AGPL-3.0 | 保护开源性，网络服务也必须开源 |
| 内容安全 | 4 项红线 + 关键词过滤 | 违法/自残/隐私/政治 |

---

> **文档结束**  
> 本规格说明书基于用户需求收集和确认生成，作为项目开发的基准文档。  
> 如需修改，请通过 PR 提交变更。
