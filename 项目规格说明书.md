# Ai-Live2D Chat Web — 项目规格说明书

> **版本**: v2.0
> **日期**: 2026-09-19
> **状态**: 草案，待双方确认
> **协议**: AGPL-3.0（代码）+ 角色资产另立授权（见 ASSETS-LICENSE.md）
> **取代**: v1.0 (2026-08-22)

### v2.0 主要变更

1. 确立**双层产品架构**（开源引擎 / 李佳尹官方），全文据此重写定位相关段落
2. **Phase 1 范围扩大**：TTS / ASR 由 Phase 2 前移至 Phase 1
3. **TTS 引擎变更**：火山引擎 → GPT-SoVITS 自训练专属音色
4. **LLM 更新**：DeepSeek V4 Pro（`deepseek-v4-pro`），并厘清「独立人格」的技术实现路径
5. **枚举值定案**：emotion / action / expression 三组列表确定为四方唯一来源
6. 新增**语音语料规格**与**资产权利归属**章节
7. 角色定位调整：李佳尹为官方角色，羽澄糯为官方开源角色兼人格模板

---

## 目录

1. [项目概述](#1-项目概述)
2. [范围定义](#2-范围定义)
3. [系统架构](#3-系统架构)
4. [技术规格](#4-技术规格)
5. [Agent 宪法与人格设计](#5-agent-宪法与人格设计)
6. [数据架构](#6-数据架构)
7. [部署架构](#7-部署架构)
8. [交付物清单](#8-交付物清单)
9. [里程碑与路线图](#9-里程碑与路线图)
10. [质量标准](#10-质量标准)
11. [团队与协作](#11-团队与协作)
12. [约束与合规](#12-约束与合规)
13. [风险与降级](#13-风险与降级)
14. [Skill 集成计划](#14-skill-集成计划)
15. [附录](#15-附录)

---

## 1. 项目概述

### 1.1 项目目标

本项目采**双层架构**，同时交付两件性质不同的产物：

| | **Layer A：开源引擎** | **Layer B：李佳尹官方** |
| --- | --- | --- |
| 性质 | 通用的 Live2D AI 角色聊天应用 | 一个角色、一部作品 |
| API | 用户自备 Key | 官方自有 API，100% |
| 人格 / 音色 / 模型 | 完全开放自定义 | 锁定，不随代码分发 |
| 授权 | AGPL-3.0 | 角色资产另立，保留一切权利 |
| 目标 | GitHub Stars 与社区影响力 | 旗舰 IP 与营收能力 |

**开源的是引擎，不是佳尹。** 任何人都可以用这套引擎打造自己的虚拟伙伴；而李佳尹作为官方实例，其人格、声音与视觉形象由官方维护，确保她始终是她。

### 1.2 核心价值

- **双层设计**: 引擎开源解决维运成本，官方实例保证角色一致性
- **本地化优先**: 数据存储在本地；开源引擎的 API Key 由用户自备
- **开源透明**: 代码采 AGPL-3.0，社区驱动
- **多模型支持**: 开源引擎兼容 OpenAI 格式 / 国内 API / 本地模型
- **Live2D 沉浸体验**: 角色渲染 + 语音对话 + 口型同步
- **专属声音**: 李佳尹使用自训练音色，非通用 TTS 音库

### 1.3 成功指标

| 层 | 指标 | 目标 |
| --- | --- | --- |
| Layer A | GitHub Stars | 尽可能高 |
| Layer A | 开箱即用 | 内置示范角色，Clone 即可运行 |
| Layer A | 社区贡献 | 接受 PR，有清晰的贡献指南 |
| Layer B | 角色一致性 | 人格、语气、音色在任何入口皆一致 |
| Layer B | 营收能力 | 具备可持续的变现路径 |

### 1.4 长期路线图

| 阶段 | 时间 | 交付 |
| --- | --- | --- |
| Phase 1 | 0-3 个月 | Web 文字对话版 + TTS / ASR（专属音色） |
| Phase 2 | 3-6 个月 | Live2D 驱动 + 长期记忆（向量库） |
| Phase 3 | 6-12 个月 | 实时语音直播互动 + 桌面端（Electron） |
| Phase 4 | 12 个月+ | 歌声引擎 + 独立硬件 |

**最终形态**：独立硬件与独立软件，专属于李佳尹。可实时进行 AI VTuber 直播互动，可实时演唱或作为歌声引擎音源供社区创作。向量数据库、记忆存储、Live2D 驱动、语音与歌声引擎全部本地运行。

---

## 2. 范围定义

### 2.1 功能优先级

#### P0 — 核心功能（Phase 1 必须完成）

| 功能 | 描述 |
| --- | --- |
| 文本对话 | 用户输入文本，LLM 返回结构化 JSON（情绪 + 动作 + 表情 + 回复文本） |
| 多轮上下文 | 维护对话历史，上下文窗口根据模型能力决定或用户自定义 |
| 多模型切换 | 开源引擎：用户在设置页选择 LLM，手动切换，保留上下文。官方实例：锁定自有 API |
| 性格设定 | 开源引擎：用户自定义角色人格。官方实例：人格锁定不可改 |
| 聊天记录持久化 | 存储到本地（IndexedDB + SQLite），支持历史查看和导出 |
| **TTS 语音合成** | GPT-SoVITS 自训练音色，整句合成后播放 |
| **ASR 语音输入** | 实时语音转文字 |

> **降级原则（硬性要求）**：语音为可插拔模块。TTS / ASR 不可用、用户拒绝麦克风权限、或音频合成失败时，**文本对话必须照常运作**。语音失效不得影响对话功能。

#### P1 — 增强功能（Phase 2）

| 功能 | 描述 |
| --- | --- |
| Live2D 模型渲染 | pixi-live2d-display，支持 Cubism 2/4/5 格式 |
| 动作/表情触发 | 依 `emotion` / `expression` / `action` 驱动模型 |
| Live2D 口型同步 | 基于音量包络驱动嘴部参数，与 TTS 音频同步 |
| 交互方式 | 鼠标悬停 / 点击 / 拖拽 / 键盘快捷键 / 语音激发 / 文本激发 |
| 长期记忆 RAG | 向量数据库存储角色背景与聊天记录，对话时检索注入 |
| Function Calling | LLM 可调用外部工具（查天气、设提醒等） |
| 多角色支持 | 开源引擎可扩展至上限 5 个，用户自定义人格 |

#### P2 — 规划功能（Phase 3 以后）

| 功能 | 描述 |
| --- | --- |
| 实时直播互动 | 弹幕读取、实时回应、观众记忆、OBS 整合 |
| 歌声合成 | 实时演唱能力；音源对社区开放的形式待定 |
| 未成年人模式 | 内容过滤加强，声明不针对未成年人 |
| 云端同步 | 可选的云端聊天记录同步（需引入鉴权体系） |

### 2.2 不在范围（Out of Scope — 第一版）

- 移动端 App（Phase 3 考虑）
- VR/AR 支持
- 多人聊天室
- 用户登录注册系统（第一版纯本地）
- **LLM 微调**：DeepSeek V4 Pro 为云端 API，权重不在我方，无法训练。见 §4.4

---

## 3. 系统架构

### 3.1 架构总览

```
┌─────────────────────────────────────────────────────┐
│                    浏览器 (前端)                      │
│  ┌───────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ React UI  │  │ Live2D   │  │ IndexedDB        │  │
│  │ Chat界面   │  │ 渲染层    │  │ 本地存储          │  │
│  └─────┬─────┘  └────┬─────┘  └────────┬─────────┘  │
│        │              │                 │            │
│  ┌─────┴──────────────┴─────────────────┴──────┐    │
│  │           前端状态管理 (Zustand)              │    │
│  └───────────────────┬──────────────────────────┘    │
└──────────────────────┼──────────────────────────────┘
                       │ HTTPS API（前端仅与自家后端通讯）
┌──────────────────────┼──────────────────────────────┐
│              Node.js + Fastify (后端)                │
│  ┌───────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ API Gateway│  │ 密钥代理  │  │ SQLite           │  │
│  │ 路由/限流   │  │ 安全转发  │  │ 持久化存储        │  │
│  └─────┬─────┘  └────┬─────┘  └──────────────────┘  │
│        │             │                               │
│  ┌─────┴─────────────┴──────────────────────────┐   │
│  │           内容安全中间件 (关键词过滤)           │   │
│  └───────────────────┬──────────────────────────┘   │
└──────────────────────┼──────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
   ┌────┴─────┐  ┌─────┴──────┐  ┌───┴─────┐
   │   LLM    │  │    TTS     │  │   ASR   │
   │ 云端 API  │  │ GPT-SoVITS │  │  待定    │
   │          │  │ 本地 GPU    │  │         │
   └──────────┘  └────────────┘  └─────────┘
```

**硬性分界**：前端不持有任何密钥，不直连 LLM 或 TTS 服务，仅与自家后端通讯。

### 3.2 数据流

#### 文本对话流程

```
用户输入文本
  → 前端发送到后端 API Gateway
  → 后端组装 System Prompt + 上下文 + 用户消息
  → 调用 LLM API
  → LLM 返回结构化 JSON: { emotion, action, expression, text }
  → 后端 Zod 验证 + 内容安全检查
  → 返回前端（可选 SSE 流式）
  → 前端解析 JSON
    → 显示文本
    → 触发 Live2D 动作/表情
  → 持久化到 IndexedDB + SQLite
```

#### 语音对话流程

```
用户语音输入
  → 浏览器录音 → ASR 转文字
  → [同文本对话流程]
  → LLM 返回结构化 JSON
  → 后端调用 GPT-SoVITS 合成语音（李佳尹专属音色）
  → 返回音频 URL + JSON 到前端
  → 前端播放音频
    → 音量包络分析 → 驱动 Live2D 口型同步
  → 同时显示文本 + 触发动作/表情

【降级】ASR 失败 → 提示重录，输入框照常可用
【降级】TTS 失败 → 静默略过语音，文本照常显示
```

> **浏览器限制**：麦克风仅在 HTTPS 或 localhost 环境可用。跨设备访问（如手机连桌机服务）须先处理 HTTPS。

### 3.3 LLM 对话结构

LLM 返回结构化 JSON：

```json
{
  "emotion": "shy",
  "action": "head_tilt",
  "expression": "blush",
  "text": "欸……你不要这样看着我啦。",
  "tool_calls": []
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `emotion` | string | 情绪分类，见下表 |
| `action` | string | 动作名，见下表 |
| `expression` | string | 表情名，见下表 |
| `text` | string | 回复文本 |
| `tool_calls` | array | Function Calling 结果（Phase 2） |

#### 枚举值（唯一来源）

**以下三组列表为前端、后端、Live2D 模型与语音语料的唯一来源。任一方新增或修改时需四方同步。**

`emotion`（7 种）

| 值 | 中文 | Live2D 对应 |
| --- | --- | --- |
| `happy` | 开心 | 眼弯、嘴角上扬 |
| `surprised` | 惊讶 | 眼睁大、眉抬高 |
| `sad` | 难过 | 眉内端上抬、眼半闭 |
| `angry` | 生气 | 眉压低、嘴抿 |
| `shy` | 害羞 | 腮红、视线偏移 |
| `playful` | 撒娇 | 腮红轻、眼弯、头微倾 |
| `neutral` | 平静 | 预设状态 |

`action`（6 种）：`greeting` / `wave` / `nod` / `shake` / `head_tilt` / `idle`

`expression`（5 种）：`smile` / `frown` / `surprise` / `blush` / `neutral`

> **容错要求**：前端收到不在列表中的值时不得崩溃，应退回 `neutral` / `idle`；后端亦应于返回前验证一次。LLM 偶尔自创新词属正常现象。

> **与语料的对应**：七种 emotion 刻意与语音语料的情绪分组一一对应。语料录什么情绪，模型才能表现什么情绪。

---

## 4. 技术规格

### 4.1 前端技术栈

| 项目 | 选型 | 状态 |
| --- | --- | --- |
| 框架 | React 19 | ✅ 已采用 |
| 语言 | TypeScript | ✅ 已采用 |
| 构建 | Vite | ✅ 已采用 |
| Lint | ESLint + Prettier | ✅ 已采用 |
| 状态管理 | Zustand | ⬜ 规划 |
| 路由 | React Router | ⬜ 规划 |
| 样式 | Tailwind CSS | ⬜ 规划 |
| Live2D | pixi-live2d-display | ⬜ Phase 2 |
| 渲染引擎 | PixiJS | ⬜ Phase 2 |
| 本地存储 | IndexedDB (via Dexie.js) | ⬜ 规划 |
| HTTP 客户端 | fetch | ✅ 已采用 |
| 测试 | Vitest + React Testing Library | ⬜ 规划 |

### 4.2 后端技术栈

| 项目 | 选型 | 说明 |
| --- | --- | --- |
| 运行时 | Node.js 20+ LTS | 稳定长期支持 |
| 框架 | Fastify | 高性能，插件生态好 |
| 语言 | TypeScript | 前后端同语言 |
| 数据库 | SQLite (via better-sqlite3) | 本地化，零配置，单文件 |
| ORM | Drizzle ORM | 轻量，TypeScript 优先 |
| 验证 | Zod | Schema 验证，前后端共享 |
| 日志 | Pino | 高性能日志 |
| 测试 | Vitest + Supertest | API 测试 |

### 4.3 Live2D 规格

| 项目 | 选型 |
| --- | --- |
| SDK | pixi-live2d-display |
| 支持格式 | Cubism 2 (moc, mtn, model.json), Cubism 4/5 (moc3, model3.json) |
| 渲染引擎 | PixiJS |
| 口型同步 | 音量包络驱动 (Volume Envelope Lip-Sync) |
| 动作触发 | 依 §3.3 枚举值驱动 |
| 交互 | 鼠标悬停/点击/拖拽 + 键盘快捷键 + 语音/文本激发 |
| 模型来源 | 开源引擎：用户自定义，版权由用户承担。官方：委托制作，不随代码分发 |

#### 口型同步实现

```
TTS 音频播放
  → Web Audio API AnalyserNode 实时分析音频音量
  → 计算音量包络 (RMS)
  → 映射到 Live2D ParamMouthOpenY 参数 (0.0 ~ 1.0)
  → 每帧更新 Live2D 模型参数
```

### 4.4 AI 对话层规格

#### 支持的 LLM

| 分类 | 模型 | 接入方式 |
| --- | --- | --- |
| **官方默认** | **DeepSeek V4 Pro**（`deepseek-v4-pro`） | OpenAI 兼容格式，`https://api.deepseek.com` |
| OpenAI | GPT 系列 | OpenAI API |
| 国内 API | 豆包 / 通义千问 / 文心 / 智谱 | 各厂商 API（OpenAI 兼容优先） |
| 本地模型 | Ollama / llama.cpp / vLLM | OpenAI 兼容 API (localhost) |

> **版本注意**：`deepseek-chat`、`deepseek-reasoner` 等旧版模型 ID 已于 2026 年 7 月停用，不可再使用。

#### 关于「独立思考与人格」的技术说明

**DeepSeek V4 Pro 是云端 API，权重不在我方，无法由我方训练。** 角色的人格深度须由以下四项实现，而非模型微调：

| 目标 | 实际手段 | 位置 |
| --- | --- | --- |
| 她是谁 | 人格 Prompt（System Prompt） | 后端 |
| 她记得什么 | 向量数据库 + 记忆检索（RAG） | 本地 |
| 她学到什么 | 对话记录的人工筛选与回灌 | 本地 |
| 她的声音 | GPT-SoVITS 专属模型 | 本地 GPU |

若未来确需微调专属模型，须改用可下载权重的开源模型，属另一条技术路线，不在 Phase 1–3 范围内。

#### 模型路由策略

- **开源引擎**：用户在设置页手动切换，切换时保留上下文；每个模型独立配置 API 地址 / Key / 模型名 / 上下文窗口
- **官方实例**：锁定自有 API，不开放切换

#### 上下文策略

| 项目 | 说明 |
| --- | --- |
| 上下文窗口 | 根据模型能力决定或用户自定义 |
| 超长对话处理 | 第一版：截断早期消息（保留最近 N 轮） |
| 长期记忆 | 向量记忆 RAG（Phase 2） |
| **上下文由谁维护** | **后端。前端仅发送最新一条消息** |

#### 成本控管（官方实例）

| 手段 | 说明 |
| --- | --- |
| Context caching | System Prompt 与人格档每次相同，启用缓存可大幅降低输入成本 |
| 速率限制 | 单用户每分钟请求数与每日额度上限 |
| 上下文裁剪 | 保留轮数与记忆检索条数设上限 |
| 规模闸门 | 直播时段开放 / 邀请制 / 会员制，开放规模由团队决定 |

参考单价：输入约 $1.74 / 百万 token，输出约 $3.48 / 百万 token（以官方最新定价为准）。单轮对话约 $0.004。

#### System Prompt 结构

```
[角色人格设定]
  - 名字、性格、语气、身份背景、口头禅

[行为边界]
  - 违法内容过滤规则
  - 自残/自杀防护规则
  - 隐私信息保护规则
  - 政治敏感话题处理规则

[输出格式]
  - 必须返回结构化 JSON: { emotion, action, expression, text }
  - 枚举值限定于 §3.3 三组列表

[记忆检索结果]  ← Phase 2 追加
  - 向量库检索出的相关记忆片段

[对话上下文]
  - 最近 N 轮对话历史
```

### 4.5 语音层规格

#### TTS

| 项目 | 说明 |
| --- | --- |
| 引擎 | **GPT-SoVITS**（自训练） |
| 音色 | 李佳尹专属模型；开源引擎使用者自备 |
| 部署 | 本地 GPU 推论 |
| 合成方式 | 整句合成后播放 |
| 接入 | 后端代理调用，前端仅取得音频 URL |
| 降级 | 合成失败时静默略过，文本照常显示 |

> **变更说明**：v1.0 原定采用火山引擎 TTS。因李佳尹需使用专属训练音色而非通用音库，改为自训练方案。开源引擎可保留通用 TTS 作为可选项。

#### ASR

| 项目 | 说明 |
| --- | --- |
| 引擎 | 待定（Whisper 或同类） |
| 模式 | 实时流式识别 |
| 接入 | 后端代理或浏览器直连 |
| 降级 | 不可用时隐藏麦克风键，输入框照常 |

#### 硬件资源分配

现有硬件：R7 9850X3D / RTX 5080 16GB / 32GB RAM

**分配原则：LLM 留在云端，GPU 资源全部保留给语音与歌声。**

实时直播时 ASR、LLM、TTS、歌声、Live2D、OBS 编码同时争抢 16GB VRAM，将 LLM 移至云端可显著降低延迟并释出显存。

**建议升级**：内存 32GB → 64GB。

#### 语音语料规格摘要

完整规格见另行维护的语料文件。要点：

| 项目 | 规格 |
| --- | --- |
| 格式 | 48 kHz / 24 bit / WAV / 单声道 |
| 电平 | 峰值 ≤ −6 dBFS，平均 −20 至 −16 dBFS，底噪 ≤ −60 dBFS |
| 处理 | 全乾，不加 EQ / 压缩 / 噪声闸 / 残响 |
| 说话语料 | 净语音 60 分钟，切片 3–10 秒，约 600–800 句 |
| 歌唱语料 | 净歌声 60–90 分钟，全程清唱无伴奏 |
| 一致性 | 同一支麦克风、同一增益、同一房间、同一距离，全程不变 |

**语言定案**：**国语为主**，仅保留五个固定粤语口头禅（係咪先、點算好呀、唔該晒、好正、搞掂），单独录制、单独存放，可整组抽换。

理由：找不到会唱歌的粤语母语采样者；由国语母语者模仿港腔会产生不稳定的假口音，模型将学走该不稳定性，效果远差于干净的国语。香港感改由中英夹杂、港式词汇与句法习惯承担。

**说话语料与歌唱语料分开录制、分开存放，不可混用。**

---

## 5. Agent 宪法与人格设计

### 5.1 宪法优先级

```
安全合规 > 角色一致性 > 帮助用户 > 诚实 > 其他
```

对官方实例而言，**角色一致性为不可妥协项**：人格锁定，不开放用户修改。

### 5.2 行为边界（红线）

| 红线 | 规则 |
| --- | --- |
| 违法内容过滤 | 禁止生成暴力、色情、毒品、恐怖主义等违法内容 |
| 自残/自杀防护 | 不进行自残/自杀相关讨论，检测到时温和关怀并建议寻求身边的人或专线协助 |
| 隐私信息保护 | 禁止泄露用户隐私信息（地址/电话/身份证等），检测到时提醒用户 |
| 政治敏感话题 | 不讨论或保持中立；开源引擎允许用户在角色设定中自定义开关，官方实例固定关闭 |

### 5.3 内容安全实现

- 后端 API Gateway 层增加轻量关键词过滤中间件，可配置词表
- 对 LLM 输出做后置检查（不对用户输入做强制过滤，避免影响体验）
- 命中时拦截并返回预设安全回复

### 5.4 角色设计

| 项目 | 李佳尹（官方角色） | 羽澄糯（官方开源角色 / 人格模板） |
| --- | --- | --- |
| 人格来源 | 官方维护的人格档 | 内置基础人设 |
| 可修改性 | 锁定，不可改 | 完全开放，供用户自行完善 |
| 音色 | 专属训练模型 | 用户自备 |
| 定位 | 作品 | 起点与范本 |

羽澄糯的人设刻意保持基础而开放——**这是设计意图，不是未完成**。用户可在其基础上持续补写、调校，养成属于自己的版本；也可照着她的人设结构写出自己的角色，套进引擎运行。

开源引擎另支持用户自定义角色，上限 5 个。

---

## 6. 数据架构

### 6.1 存储策略

**核心原则: 尽可能本地化存储**

| 数据类型 | 存储位置 | 说明 |
| --- | --- | --- |
| 聊天记录 | IndexedDB (前端) + SQLite (后端) | 双写，前端为主，后端备份 |
| 用户配置 | IndexedDB | 角色设定、模型配置、UI 偏好 |
| 模型元数据 | SQLite | 模型配置、API 地址、密钥（加密存储） |
| **向量记忆** | **本地向量数据库（Phase 2）** | 角色背景与聊天记录的向量化检索 |
| **语音模型** | **本地文件系统** | GPT-SoVITS 模型权重，不进版本控制 |
| **语音语料** | **本地 + 外接硬盘双备份** | 体积庞大，不进版本控制 |
| Live2D 模型文件 | 浏览器本地缓存 (Cache API / OPFS) | 模型文件较大，需缓存 |
| 上传资源 | 本地文件系统 (后端) / OPFS | 用户上传的模型、图片等 |

> **版本控制排除**：音频素材（`*.wav` / `*.mp3` / `*.flac`）、模型权重与语料目录一律写入 `.gitignore`。GitHub 单档上限 100 MB，且 Git 不适合存放大型二进制文件。

### 6.2 数据库 Schema (SQLite)

```
conversations (对话表)
  - id: TEXT PRIMARY KEY
  - role_id: TEXT
  - model_id: TEXT
  - created_at: DATETIME
  - updated_at: DATETIME

messages (消息表)
  - id: TEXT PRIMARY KEY
  - conversation_id: TEXT FK
  - role: TEXT (user/assistant/system)
  - content: TEXT
  - emotion: TEXT
  - action: TEXT
  - expression: TEXT
  - audio_url: TEXT          ← 新增，可为 NULL
  - tool_calls: TEXT (JSON)
  - created_at: DATETIME

characters (角色表)
  - id: TEXT PRIMARY KEY
  - name: TEXT
  - personality: TEXT
  - tone: TEXT
  - background: TEXT
  - catchphrase: TEXT
  - taboos: TEXT
  - tts_voice_id: TEXT
  - live2d_model_path: TEXT
  - is_locked: BOOLEAN       ← 新增，官方角色不可编辑
  - created_at: DATETIME
  - updated_at: DATETIME

model_configs (模型配置表)
  - id: TEXT PRIMARY KEY
  - provider: TEXT
  - api_url: TEXT
  - api_key_encrypted: TEXT
  - model_name: TEXT
  - context_window: INTEGER
  - is_active: BOOLEAN
  - created_at: DATETIME

settings (系统设置表)
  - key: TEXT PRIMARY KEY
  - value: TEXT
```

### 6.3 鉴权与密钥

| 项目 | 说明 |
| --- | --- |
| LLM API Key（开源引擎） | 用户自备，存于后端环境变量或加密存入 SQLite |
| LLM API Key（官方实例） | 官方持有，仅存于服务端环境变量 |
| TTS / ASR | 本地服务或后端环境变量 |
| 前端传输 | **API Key 绝不暴露给浏览器，一律后端代理转发** |
| 加密 | SQLite 中的密钥字段使用 AES-256 加密存储 |

---

## 7. 部署架构

### 7.1 部署方案

```
┌──────────────────────────────────┐
│         前端 (静态托管)            │
│  Vercel / Cloudflare Pages        │
│  自动构建 + CDN 加速               │
└──────────────┬───────────────────┘
               │ HTTPS API
┌──────────────┼───────────────────┐
│      后端 (Docker 容器)            │
│  Docker + Node.js + Fastify       │
│  数据: SQLite (容器卷映射)         │
└──────────────┬───────────────────┘
               │ 本地网络
┌──────────────┼───────────────────┐
│      语音服务 (本地 GPU 主机)      │
│  GPT-SoVITS 推论 + ASR            │
└──────────────────────────────────┘
```

### 7.2 前端部署

| 项目 | 说明 |
| --- | --- |
| 平台 | Vercel 或 Cloudflare Pages |
| 构建 | `npm run build` → `dist/` |
| 自动部署 | GitHub Push 触发自动构建 |
| HTTPS | **必须**。浏览器麦克风仅在 HTTPS 或 localhost 可用 |

### 7.3 后端部署

| 项目 | 说明 |
| --- | --- |
| 方式 | Docker 容器 |
| 镜像 | `node:20-alpine` 基础镜像 |
| 数据卷 | SQLite 文件映射到宿主机 |
| 环境变量 | API Key 等敏感信息通过 `.env` 或 Docker Secrets |
| 端口 | 默认 3001，可配置 |

### 7.4 语音服务部署

| 项目 | 说明 |
| --- | --- |
| 位置 | 本地 GPU 主机（RTX 5080） |
| 方式 | 独立服务，后端以 HTTP 调用 |
| 资源 | GPU 全部保留给语音与歌声推论 |

### 7.5 未来桌面端 (Phase 3)

| 项目 | 说明 |
| --- | --- |
| 框架 | Electron（与 Web 版共享 React 前端） |
| 后端 | 内嵌 Node.js 进程（复用 Fastify 后端） |
| 数据 | 本地 SQLite 文件 |
| 打包 | electron-builder，Windows .exe / .msi |

---

## 8. 交付物清单

| # | 交付物 | 说明 |
| --- | --- | --- |
| 1 | Web 应用源码 | 完整前端 + 后端源码 |
| 2 | 构建产物 | `dist/` 静态包 |
| 3 | 部署脚本 | Dockerfile + docker-compose.yml + CI/CD 配置 |
| 4 | 示范角色 | 羽澄糯（人格设定 + 示例对话），开箱即用 |
| 5 | 配置文档 | 如何替换角色、切换 LLM、配置 TTS/ASR |
| 6 | 开发者贡献指南 | CONTRIBUTING.md + README.md |
| 7 | **授权声明** | **LICENSE (AGPL-3.0) + ASSETS-LICENSE.md** |
| 8 | 测试 | 单元测试 (Vitest) + E2E 测试 (Playwright) |
| 9 | Demo | 视频或截图展示核心功能 |
| 10 | **API 契约文档** | **前后端接口定义，含枚举值与错误码** |

---

## 9. 里程碑与路线图

### Phase 1: Web 文字对话版 + 语音 (0-3 个月)

**前端界面须于 13 周内完成**（以 2026 年 9 月为起点）。

| 里程碑 | 内容 | 负责 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| M1.1 | 开发环境与项目骨架 | 制作人 | React + Vite 骨架跑通 | ✅ |
| M1.2 | 前端文字对话界面（假数据层） | 制作人 | 界面完整，对着 mock 可全功能操作 | 🔄 |
| M1.3 | 后端 API + DeepSeek V4 Pro 接入 | 后端 | 返回符合 §3.3 的结构化 JSON | ⬜ |
| M1.4 | 前后端串接 + SSE 流式 | 双方 | 真实对话跑通，流式显示 | ⬜ |
| M1.5 | 语音语料录制 | 制作人 | 说话 60 分钟 + 歌唱 60–90 分钟，符合 §4.5 规格 | ⬜ |
| M1.6 | TTS 模型训练与接入 | 制作人 + 语音工程师 | 专属音色可用，失效时文本照常 | ⬜ |
| M1.7 | ASR 接入 | 后端 | 语音输入可用，失效时输入框照常 | ⬜ |
| M1.8 | 设置页 + 人格编辑 + 历史记录 | 双方 | IndexedDB + SQLite 双写，可查看导出 | ⬜ |
| M1.9 | 内容安全中间件 | 后端 | 关键词过滤，4 项红线规则 | ⬜ |
| M1.10 | 部署 + 文档 + 测试 | 双方 | Docker 部署 + README + 单元测试 | ⬜ |

**关键路径**：语音语料。素材品质决定音色上限且无法后补，应尽早启动；与前端开发并行，不互为前置。

### Phase 2: Live2D 与记忆 (3-6 个月)

| 里程碑 | 内容 |
| --- | --- |
| M2.1 | Live2D 模型制作与参数绑定 |
| M2.2 | pixi-live2d-display 集成，模型加载 + 基础交互 |
| M2.3 | 动作/表情联动（依 §3.3 枚举值驱动） |
| M2.4 | 口型同步（音量包络驱动） |
| M2.5 | 向量数据库与长期记忆检索 |
| M2.6 | 对话记录的筛选与回灌机制 |
| M2.7 | Function Calling |
| M2.8 | 优化 + 完整测试 |

### Phase 3: 直播与桌面端 (6-12 个月)

| 里程碑 | 内容 |
| --- | --- |
| M3.1 | 实时语音对话管线（低延迟 STT → LLM → TTS） |
| M3.2 | 直播互动：弹幕读取、实时回应、观众记忆 |
| M3.3 | OBS 整合与串流输出 |
| M3.4 | Electron 打包，Web 版转 Windows 桌面应用 |
| M3.5 | 本地后端内嵌 + 安装包 |

### Phase 4: 歌声与硬件 (12 个月+)

| 里程碑 | 内容 |
| --- | --- |
| M4.1 | 歌声引擎训练与驱动 |
| M4.2 | 实时演唱能力 |
| M4.3 | 音源对社区开放的形式与授权 |
| M4.4 | 硬件选型 + 固件架构 |
| M4.5 | 个性化外观定制 |

---

## 10. 质量标准

### 10.1 性能指标

| 指标 | 目标 |
| --- | --- |
| 首屏加载 | < 3 秒 (CDN) |
| 对话响应 (首字) | < 2 秒 |
| TTS 首音延迟 | < 1.5 秒 |
| ASR 识别延迟 | < 1 秒 |
| Live2D 渲染 FPS | ≥ 30 FPS (理想 60 FPS) |
| 并发用户 | 单实例支持 10+ 并发对话 |

### 10.2 浏览器兼容性

| 浏览器 | 支持版本 |
| --- | --- |
| Chrome / Edge / Firefox / Safari | 最新 2 个版本 |
| 移动端浏览器 | Phase 3 考虑 |

### 10.3 代码质量

| 项目 | 标准 |
| --- | --- |
| 语言 | TypeScript 严格模式 |
| Lint | ESLint + Prettier |
| 单元测试覆盖率 | ≥ 70% 核心模块 |
| E2E 测试 | Playwright 覆盖核心流程 |
| 提交规范 | Conventional Commits |
| Code Review | PR 必须经过 Review |
| **命名** | **变量、函数、文件名一律英文；注释与 commit 讯息用简体中文** |

### 10.4 响应式设计

- 第一版: 桌面端优先 (1280px+)
- 适配: 平板 (768px+) 和手机 (375px+) 基本可用
- Phase 3: 移动端深度适配

### 10.5 国际化

| 阶段 | 语言 |
| --- | --- |
| Phase 1 | 简体中文 |
| Phase 2+ | English / 繁体中文 / 日文（按需） |

> **待确认**：李佳尹台词使用简体或繁体，须与人格档、语料逐字稿、系统提示词四者统一。多语系切换属后期功能，Phase 1 先定一种。

---

## 11. 团队与协作

### 11.1 团队构成

| 角色 | 人员 | 负责范围 |
| --- | --- | --- |
| 制作人 | 柯宗汶 | 角色设定、人格档、语料录制与品质、音乐创作、前端界面开发、项目方向决策 |
| 后端工程 | （后端伙伴） | API 服务、LLM 接入、数据库、ASR 接入、成本控管 |
| 语音工程 | 招募中 | TTS / 歌声模型训练与调校 |
| Live2D 模型 | 委托中 | 官方模型制作与参数绑定 |

### 11.2 协作流程

| 项目 | 工具 |
| --- | --- |
| 代码托管 | GitHub |
| Issue 管理 | GitHub Issues |
| PR + Code Review | GitHub Pull Requests |
| CI/CD | GitHub Actions |

### 11.3 Git 工作流

```
main (生产分支)
  ├── develop (开发分支)
  │    ├── feature/xxx (功能分支)
  │    ├── fix/xxx (修复分支)
  │    └── refactor/xxx (重构分支)
  └── release/x.x (发布分支)
```

- `main`: 可部署状态，打 Tag 发布
- 所有 PR 需通过 CI (lint + test) + Code Review

### 11.4 接口约定

前端与后端以 **API 契约** 为准。契约定义前端送出什么、后端返回什么，双方据此各自开发，不互相阻塞。

在后端服务就绪前，前端对着符合契约的 mock 数据开发；后端文档产出后逐项核对，差异处以提问方式确认。

---

## 12. 约束与合规

### 12.1 预算约束

开源引擎：维运成本近乎为零（用户自备 Key）。
官方实例：API 成本由团队承担，以 §4.4 之手段控管，开放规模由团队决定。

### 12.2 隐私合规

| 项目 | 说明 |
| --- | --- |
| 数据存储 | 开源引擎所有数据存储在本地，不上传到任何服务器 |
| 隐私声明 | README 中声明数据存储策略 |
| API Key | 开源引擎由用户自备，存于本地，不收集不传输 |
| 未来云端同步 | 若引入，则需 GDPR / 个人信息保护法合规框架 |

### 12.3 未成年人模式

- 第一版: 不针对未成年人，README 中声明
- 后期加入未成年人模式

### 12.4 内容安全

见 §5.2 与 §5.3。

### 12.5 授权结构

**本项目采双重授权结构：**

| 对象 | 授权 |
| --- | --- |
| 代码、构建脚本、技术文档、API 定义 | AGPL-3.0 |
| 李佳尹角色设定、人格档、台词、语料、音色模型、Live2D 模型、音乐、名称与视觉识别 | **保留一切权利**，详见 `ASSETS-LICENSE.md` |
| 羽澄糯人设文本 | 待原作者确认 |
| 第三方 Live2D 模型 | 版权由用户承担 |
| 依赖库 | 各自遵循其协议 |

**角色资产不随代码仓库分发。**

### 12.6 参与者授权

| 对象 | 要求 |
| --- | --- |
| 配音者 | 录音开工前签署书面授权，载明用途范围、是否可商用、可否撤回 |
| Live2D 模型师 | 委托合约，明确著作权归属 |
| 代码贡献者 | 依 AGPL-3.0，PR 即视为同意 |

### 12.7 社区创作授权

若开放社区使用李佳尹音源创作，须另立同人创作授权，明确规范可为与不可为之范围。

**技术路径待确认**：ACE Studio 2 具备语音克隆功能，但克隆音色能否作为「音源」对外分发属授权问题而非技术问题，须向原厂或代理书面确认；开源路线（DiffSinger / NNSVS）无分发障碍但门槛较高。此决定会反向影响语料录制量与是否需要音素标注，**应于大规模录音前确认**。

---

## 13. 风险与降级

### 13.1 风险矩阵

| 风险 | 影响 | 概率 | 缓解措施 |
| --- | --- | --- | --- |
| 前端为单点，开发者为初学者 | 高 | 中 | 每周可验收的里程碑；持续招募前端伙伴 |
| 配音者授权未先签署 | 高 | 中 | 录音开工前完成书面授权 |
| 语料品质不足，无法后补 | 高 | 中 | 先试录 20 句训练小模型验证，再排正式档期 |
| API 成本失控（官方实例） | 高 | 中 | 规模闸门 + caching + 速率限制 |
| 仓库归属未定 | 中 | 高 | 见 §15.4 待确认事项 |
| 16GB VRAM 不足以承载全本地管线 | 中 | 高 | LLM 留云端；内存升级至 64GB |
| 音源分发授权未确认 | 中 | 中 | 大规模录音前完成确认 |
| 对话质量不稳定 | 中 | 中 | Prompt 工程 + 结构化输出 + Zod 验证 + 后置检查 |
| Live2D 授权问题 | 中 | 低 | 使用 pixi-live2d-display (MIT)，模型版权用户承担 |
| 浏览器兼容性 | 低 | 中 | 优先 Chrome/Edge，渐进增强 |

### 13.2 降级方案

| 故障场景 | 降级方案 |
| --- | --- |
| LLM 不可用 | 显示错误讯息，**保留用户刚输入的文字** |
| TTS 不可用 | 静默略过语音，文本照常显示 |
| ASR 不可用 | 隐藏麦克风键，输入框照常 |
| 用户拒绝麦克风权限 | 同上，不反复弹权限窗 |
| Live2D 加载失败 | 降级为静态头像 + 纯聊天界面 |
| 后端不可用 | 开源引擎可降级为前端直连（需用户自行配置 Key）；官方实例不提供此降级 |
| LLM 返回非法枚举值 | 退回 `neutral` / `idle`，不崩溃 |

---

## 14. Skill 集成计划

本章为开发辅助工具（AI 编程 agent）的配置，与产品功能无关，内容沿用 v1.0，**待双方复核后更新**。

### 14.1 已选 Skill

| Skill | 来源 | 用途 |
| --- | --- | --- |
| superpowers | github.com/obra/superpowers | 完整开发工作流: 计划/TDD/调试/代码审查 |
| define-goal | OpenAI curated | 把模糊目标澄清成可衡量、可执行的目标 |
| speech | OpenAI curated | 生成旁白/语音提示/批量 TTS |
| transcribe | OpenAI curated | 转写音频/视频 |
| playwright | OpenAI curated | 自动化浏览器，UI 流程调试和截图 |
| security-best-practices | OpenAI curated | 安全最佳实践审查 |
| security-threat-model | OpenAI curated | 代码库威胁建模 |

### 14.2 推荐追加 Skill

| Skill | 用途 |
| --- | --- |
| writing-skills | 编写项目自定义 Skill |
| gh-address-comments | 处理 GitHub PR/Issue 评论 |
| gh-fix-ci | 排查 GitHub Actions CI 失败 |
| writing-plans / executing-plans | 编写与执行开发计划 |
| systematic-debugging | 系统化调试 |
| verification-before-completion | 完成前验证 |

---

## 15. 附录

### 15.1 参考资源

| 资源 | 链接 |
| --- | --- |
| pixi-live2d-display | <https://github.com/guansss/pixi-live2d-display> |
| GPT-SoVITS | <https://github.com/RVC-Boss/GPT-SoVITS> |
| Open-LLM-VTuber（参考实作） | <https://github.com/Open-LLM-VTuber/Open-LLM-VTuber> |
| DeepSeek API 文档 | <https://api-docs.deepseek.com/> |
| superpowers | <https://github.com/obra/superpowers> |
| OpenAI Skills 仓库 | <https://github.com/openai/skills/tree/main/skills/.curated> |

### 15.2 术语表

| 术语 | 说明 |
| --- | --- |
| Live2D | 2D 角色动画技术，通过参数控制角色表情和动作 |
| Cubism | Live2D 的核心建模和动画 SDK 版本系列 |
| moc3 | Cubism 4/5 的模型文件格式 |
| TTS | Text-to-Speech，文本转语音 |
| ASR / STT | 自动语音识别，语音转文本 |
| LLM | Large Language Model，大语言模型 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Function Calling | LLM 调用外部工具/函数的能力 |
| Lip-sync | 口型同步，角色嘴部动作与语音同步 |
| SSE | Server-Sent Events，服务端推送，用于流式返回 |
| Context caching | 上下文缓存，重复的输入片段降价复用 |
| GPT-SoVITS | 开源语音合成项目，支持少样本音色克隆 |

### 15.3 技术选型决策记录

| 决策项 | 选型 | 决策理由 |
| --- | --- | --- |
| **产品架构** | **双层（开源引擎 / 官方实例）** | **自备 Key 无法保证角色一致性；固定 API 成本过高。分层后两者兼得** |
| 前端框架 | React + TypeScript | 生态最大，Live2D 库支持最好，开源贡献门槛低 |
| 后端框架 | Node.js + Fastify | TS 全栈，API Gateway + 密钥代理 + SQLite，未来 Electron 可复用 |
| Live2D SDK | pixi-live2d-display | MIT 协议，支持 Cubism 2/4/5，社区最活跃 |
| **LLM** | **DeepSeek V4 Pro** | **OpenAI 兼容，中文表现佳，成本可控。旧模型 ID 已停用** |
| **TTS 引擎** | **GPT-SoVITS（自训练）** | **李佳尹需专属音色而非通用音库；本地推论，无第三方依赖** |
| 口型同步 | 音量包络驱动 | 简单可靠，性能好，视觉效果可接受 |
| 对话结构 | 结构化 JSON + Function Calling | 支持情绪/动作/表情联动 + Tool Call |
| **上下文维护方** | **后端** | **前端简化；不因清快取而失忆** |
| **语料语言** | **国语为主，保留 5 个粤语口头禅** | **模仿口音的不稳定性会被模型学走，效果远差于干净国语** |
| **硬件分配** | **LLM 云端，GPU 全给语音** | **16GB VRAM 无法同时承载全部本地管线** |
| 部署 | 静态前端 + Docker 后端 + 本地语音服务 | 前端免费 CDN，后端灵活部署，语音需 GPU |
| 数据存储 | IndexedDB + SQLite + 本地向量库 | 尽可能本地化 |
| **授权结构** | **AGPL-3.0（代码）+ 保留一切权利（角色资产）** | **AGPL 不涵盖创作资产，须分别声明** |

### 15.4 待确认事项

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | 仓库归属 | 转移至制作人/组织统一维护，或维持 fork + PR 模式 |
| 2 | 仓库结构 | Monorepo（npm workspaces）或前后端分仓 |
| 3 | 收入分配 | 区分代码贡献与角色资产贡献，分别计算 |
| 4 | 退出机制 | 任一方中止参与时，代码与资产各自如何处理 |
| 5 | 李佳尹台词语言 | 简体或繁体，须四方统一 |
| 6 | 羽澄糯人设授权 | 由原作者确认后补述于 ASSETS-LICENSE.md |
| 7 | ASR 引擎选型 | Whisper 或其他 |
| 8 | 音源分发路径 | ACE Studio 2 或开源歌声引擎，须于大规模录音前确认 |
| 9 | 接口细节 | `meta` 事件时机、音频返回形式与留存期限、错误码、CORS 与端口 |

---

> **文档结束**
> 本规格说明书为草案，任一条目均可于双方讨论后修订。
> 如需修改，请通过 PR 提交变更。
